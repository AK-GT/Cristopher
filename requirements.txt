# CRISTOPHER — Fase 1 (MVP del bucle). Dependencias mínimas.
google-genai>=1.0.0     # SDK unificado de Gemini: gemini-2.5-flash + function calling
python-dotenv>=1.0.0    # carga GEMINI_API_KEY desde .env
ddgs>=6.0.0             # búsqueda web (DuckDuckGo) gratis, sin API key
