# CRISTOPHER — dependencias.
google-genai>=1.0.0     # SDK unificado de Gemini: cerebro + function calling
python-dotenv>=1.0.0    # carga GEMINI_API_KEY desde .env
ddgs>=6.0.0             # búsqueda web (DuckDuckGo) gratis, sin API key

# Fase 4 — Integraciones Google (Calendar + Gmail vía OAuth).
google-api-python-client>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.2.0
# (La búsqueda de élite Tavily usa `requests`, que ya viene con google-genai.)

# Fase 5 — Navegador. Tras instalar: `playwright install chromium`.
playwright>=1.40.0

# Fase 6 — Voz. STT (faster-whisper), TTS (piper-tts), audio (sounddevice), micrófono
# (pynput). Descargar la voz: python -m piper.download_voices es_ES-davefx-medium
#   --download-dir data/voice/piper
faster-whisper>=1.0.0
piper-tts>=1.2.0
sounddevice>=0.4.6
numpy>=1.24.0
pynput>=1.7.0

# Fase 8 — HUD. Métricas de sistema. three.js va vendorizado en
# cristopher/hud/static/three.min.js (offline, sin CDN).
psutil>=5.9.0

# Arranque por 2 palmadas — reproducir el MP3 de arranque sin abrir ventana.
# Se fija ==1.2.2 porque 1.3.0 está roto al instalar en Windows/Python 3.11.
playsound==1.2.2

# Módulo de música (Tanda A) — motor de reproducción propio.
# REQUISITO DEL SISTEMA: VLC de 64 bits instalado (para libvlc; debe coincidir en bits
# con Python). Instálalo desde videolan.org o `winget install VideoLAN.VLC`.
python-vlc>=3.0.0       # control fino de VLC desde Python (cualquier formato)
yt-dlp>=2024.1.1        # resuelve y extrae el audio de "cualquier canción" de la web
